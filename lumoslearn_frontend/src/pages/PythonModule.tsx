// Python module was intentionally removed from the app.
// Keeping a tiny stub component so any leftover imports won't break the build.
// Deleted placeholder: Python module fully removed.
export {};